interface:
  display_name: "Handoff"
  short_description: "Draft continuation prompts"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  default_prompt: "Use $handoff."

policy:
  # Manual-only: do not inject this skill into agent context.
  # It remains available only when explicitly invoked as $handoff.
  allow_implicit_invocation: false
